{
  "buildCommand": "npm install && node build-data.js && cd client && npm install && npm run build && cd ..",
  "outputDirectory": "client/dist",
  "includeFiles": [
    "data/**",
    "api/**"
  ],
  "routes": [
    { "src": "/assets/(.*)", "dest": "/assets/$1" },
    { "src": "/api/(.*)", "dest": "/api" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "crons": [
    {
      "path": "/api/refresh",
      "schedule": "*/5 * * * *"
    }
  ]
}
